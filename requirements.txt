# Slack Bot Framework
slack-bolt>=1.18.0,<2.0.0
slack-sdk>=3.26.0,<4.0.0

# Web Framework
fastapi>=0.109.0,<1.0.0
uvicorn[standard]>=0.27.0,<1.0.0

# Legacy (will be removed)
flask>=3.0.0,<4.0.0

# Scheduling
apscheduler>=3.10.0,<4.0.0

# Data Validation
pydantic>=2.5.0,<3.0.0
pydantic-settings>=2.1.0,<3.0.0

# HTTP Client for Tencent API
requests>=2.31.0,<3.0.0

# Tencent Cloud SDK (full package includes all services)
tencentcloud-sdk-python>=3.0.1000
# StreamPackage SDK (MDP) for input status verification
# Note: MDP SDK may be included in main SDK, but explicitly install if needed
# tencentcloud-sdk-python-mdp>=3.0.1000
# CSS (Live) SDK - included in main SDK, but explicitly install if needed
# tencentcloud-sdk-python-live>=3.0.1000

# Environment Variables
python-dotenv>=1.0.0,<2.0.0

# Logging
structlog>=24.1.0,<25.0.0

# MCP (Model Context Protocol)
mcp>=1.0.0

# AI Assistant (Claude API)
anthropic>=0.18.0

# Development
pytest>=7.4.0,<8.0.0
black>=23.12.0,<24.0.0
flake8>=7.0.0,<8.0.0
